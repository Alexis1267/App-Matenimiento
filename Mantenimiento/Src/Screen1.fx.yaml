Screen1 As screen:
    OnVisible: |
        =Set(BtnPruebaA,false);
        UpdateContext({BTNPRUEBAA: false});
        
        
        Set(TiempoActual, LookUp(PreventivSure_Preg,Folio_Maq = ID_Maquina,TempoHH));
        
        
        Set(Horas, Hour(Value(LookUp(PreventivSure_Preg,Folio_Maq = ID_Maquina,TempoHH))));
        Set(Minutos, Minute(Value(LookUp(PreventivSure_Preg,Folio_Maq = ID_Maquina,TempoHH))));
        Set(Segundos, Second(Value(LookUp(PreventivSure_Preg,Folio_Maq = ID_Maquina,TempoHH))));

    Dropdown2 As dropdown:
        Items: =[LookUp(PreventivSure_Preg,Folio_Maq=36).ACT1]
        X: =84
        Y: =54
        ZIndex: =1

    Dropdown2_1 As dropdown:
        Default: =" Coalesce(LookUp(PreventivSure_Preg, Folio_Maq = 36).ACT1)"
        Items: |-
            =["R","N", Coalesce(LookUp(PreventivSure_Preg, Folio_Maq = 36).ACT1)]
            
            //["R","N", Coalesce(LookUp(PreventivSure_Preg, ID_Preg = 9 || folio_Maq = Id_Maquina).Act11)]
        X: =84
        Y: =125
        ZIndex: =2

    Dropdown4 As dropdown:
        Default: =Coalesce(LookUp(PreventivSure_Preg, Folio_Maq = 36).ACT3)
        Items: |
            =["R","N", Coalesce(LookUp(PreventivSure_Preg, Folio_Maq = 36).ACT3)]
        X: =84
        Y: =228
        ZIndex: =3

    Button9 As button:
        DisplayMode: =If(BtnPruebaA,DisplayMode.Disabled,DisplayMode.Edit)
        OnSelect: =Set(BtnPruebaA,true)
        Text: ="Button"
        X: =1065
        Y: =96
        ZIndex: =4

    Icon4 As icon.Cancel:
        Icon: =Icon.Cancel
        OnSelect: =Set(BtnPruebaA,false)
        X: =807
        Y: =84
        ZIndex: =5

    Label10 As label:
        Text: ="VAR  ""SET"""
        X: =925
        Y: =30
        ZIndex: =6

    Label10_1 As label:
        Text: ="VAR  ""UpdateContext"""
        Width: =212
        X: =915
        Y: =188
        ZIndex: =7

    Button10 As button:
        DisplayMode: =If(BTNPRUEBAA,DisplayMode.Disabled,DisplayMode.Edit)
        OnSelect: |-
            =UpdateContext({BTNPRUEBAA: true})
        Text: ="Button"
        X: =1065
        Y: =268
        ZIndex: =8

    Icon5 As icon.Cancel:
        Icon: =Icon.Cancel
        OnSelect: |-
            =UpdateContext({BTNPRUEBAA: false})
        X: =807
        Y: =268
        ZIndex: =9

    Label14 As label:
        Align: =Align.Center
        Height: =48
        Text: =LookUp(PreventivSure_Preg,Folio_Maq = ID_Maquina,TempoHH)
        Width: =182
        X: =559
        Y: =467
        ZIndex: =11

    Timer6 As timer:
        AutoPause: =false
        AutoStart: =true
        Start: =true
        Text: |-
            =Text(TimeValue(Label14.Text), "hh:mm:ss")
        X: =168
        Y: =395
        ZIndex: =12

